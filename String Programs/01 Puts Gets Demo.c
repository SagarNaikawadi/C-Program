#include<stdio.h>
#include<comcat.h>

int main()
{
    char cArr[50] ={'\0'};

    puts("\n Enter The String => ");
    gets(cArr);

    puts("\n Given String is ");
    puts(cArr);

    getch();
    return 0;
}
